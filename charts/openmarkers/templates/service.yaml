apiVersion: v1
kind: Service
metadata:
  name: {{ include "openmarkers.fullname" . }}
  labels:
    {{- include "openmarkers.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openmarkers.selectorLabels" . | nindent 4 }}
